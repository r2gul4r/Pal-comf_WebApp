# palAuto 에이전트 작업 규칙

## 시작 전

- 작업 전에 루트 `PLAN.md`를 읽고 현재 단계와 범위를 확인한다.
- 현재 우선순위는 팰월드 1.0 설치 데이터를 이용한 로컬 웹 역검색기 MVP다.
- 웹앱 MVP 제출이 끝나기 전까지 게임 모드 구현은 범위 밖이다.

## 데이터 기준

- 사용자의 현재 Steam 팰월드 설치본을 1차 기준 데이터로 사용한다.
- 예전 버전 문서, 위키, 알려진 자산 경로와 매핑은 탐색 후보일 뿐 사실로 단정하지 않는다.
- 자산 경로, 필드 의미와 매핑 호환성은 현재 pak 인덱스와 추출 결과로 검증한다.
- 구조화된 게임 데이터가 있으면 현지화 설명문 파싱보다 우선한다.
- 추론하거나 수동 확인한 값은 구조화된 원본 값과 구분해 근거 상태를 남긴다.

## 게임 파일 안전

- 게임 설치 폴더, pak과 세이브 파일은 읽기 전용으로 취급한다.
- 게임 원본 파일이나 세이브를 수정, 교체, 이동 또는 삭제하지 않는다.
- 대용량 pak 전체를 풀지 않고 필요한 자산만 선택 추출한다.
- 원본 `uasset`, `uexp`, `locres`, 텍스처와 추출 캐시는 Git에 커밋하지 않는다.
- 추출 도구가 게임 설치 경로에 출력 파일을 만들지 않게 한다.

## 데이터 무결성

- 확인되지 않은 수치, 효과 범위와 중첩 여부를 지어내지 않는다.
- 모르는 값은 `unknown` 또는 `null`로 유지하며 임의로 `0`이나 `false`로 바꾸지 않는다.
- 팰 특성, 파트너 스킬과 팰 자체 작업 적성을 서로 혼동하지 않는다.
- 자기 자신 대상, 거점의 다른 팰 대상과 거점 전체 대상을 분리한다.
- 하나의 스킬에 효과가 여러 개면 효과 단위로 분리해 정규화한다.
- 정규화 결과에는 가능한 경우 원문 설명과 출처 자산 경로를 함께 보존한다.

## 구조

- 데이터 추출기와 웹 UI를 분리한다.
- 웹앱은 생성된 정적 JSON과 웹용 이미지만 읽으며 pak을 직접 읽지 않는다.
- 반복 가능한 추출 도구는 `tools/extractor` 아래에 둔다.
- 생성 데이터는 `data/generated`, 웹용 이미지는 `public/generated` 아래에 둔다.
- 게임 원본과 대용량 생성물은 `.gitignore`로 차단한다.

## 구현 순서

1. 현재 1.0 pak 인덱스와 매핑 호환성을 확인한다.
2. 팰 3~5종의 이름, 작업 적성, 파트너 스킬, 거점 관련 특성과 썸네일을 샘플 추출한다.
3. 샘플과 실제 게임 화면을 대조한 뒤 반복 가능한 전체 추출기를 만든다.
4. 검증된 생성 데이터를 기준으로 검색 UI를 구현한다.

샘플 추출 성공 전에는 UI를 실제 데이터처럼 보이는 임시 값으로 채우지 않는다.

## 검증

- 데이터 ID 중복과 참조 무결성을 검사한다.
- 한국어 이름과 설명의 현지화 키 결합을 검사한다.
- 썸네일 매핑이 실제 팰과 일치하는지 샘플로 대조한다.
- 변경 범위에 맞는 테스트를 실행한다.
- 웹앱 작업 완료 전 아래 명령을 통과시킨다.

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

## 과제 기록

- 중요한 AI 요청, 제안, 의심점, 검증 방법, 오류와 수정 결과를 `docs/AI_WORKLOG.md`에 기록한다.
- 실패 사례를 꾸며내지 않고 실제 발생한 오류만 기록한다.
- 빌드 ID, 추출 시각과 데이터 출처 경로를 생성 데이터 메타정보에 남긴다.

## Git

- 한 커밋에는 하나의 논리적 변경만 담는다.
- 게임 원본, 비밀정보, 로컬 절대경로가 포함된 캐시와 대용량 추출물을 스테이징하지 않는다.
- 사용자의 기존 변경을 덮어쓰거나 되돌리지 않는다.
